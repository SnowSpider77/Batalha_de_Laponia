{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build files",
            "type": "shell",
            "command": "cmake --build ./build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": "$gcc"
        }
    ]
}